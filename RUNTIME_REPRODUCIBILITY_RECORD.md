# Runtime Reproducibility Record

This record is generated by `scripts/export_runtime_record.py` on the host used for local LLM inference. It is intended to preserve exact runtime and model metadata for JAIR reproducibility review.

Generated at UTC: `2026-06-04T17:34:39Z`

## Deterministic inference controls

- `temperature`: `0`
- `max_generation_tokens`: `1024`
- `context_window_tokens`: `32768`
- `qwen3_thinking_disabled`: `True`
- `recommended_ollama_num_parallel`: `1`

## Platform

- `platform`: `Linux-6.8.0-49-generic-x86_64-with-glibc2.35`
- `python_executable`: `/usr/bin/python`
- `cwd`: `/workspace/long_context_cue`

## Package versions

- `python`: `3.11.10 (main, Sep  7 2024, 18:35:41) [GCC 11.4.0]`
- `torch`: `2.4.1+cu124`
- `transformers`: `not installed`
- `sentence-transformers`: `not installed`
- `pandas`: `not installed`
- `numpy`: `1.26.3`
- `scipy`: `not installed`
- `scikit-learn`: `not installed`
- `pyyaml`: `6.0.2`
- `tqdm`: `not installed`

## CUDA / GPU

```json
{
  "torch_importable": true,
  "cuda_available": true,
  "devices": [
    {
      "index": 0,
      "name": "NVIDIA A40",
      "total_memory_bytes": 47708110848,
      "total_memory_gb": 44.43,
      "major": 8,
      "minor": 6
    }
  ]
}
```

### nvidia-smi query

```text
Field "cuda_version" is not a valid field to query.
```

## Ollama

### ollama --version
```text


[stderr]
ollama not found
```

### ollama list
```text


[stderr]
ollama not found
```

## Model records

Each block preserves the local `ollama show --verbose` and `ollama show` output for the evaluated tag. These outputs should include digest, parameter, and quantization metadata when exposed by the installed Ollama version and local model store.

### qwen2.5:14b

#### ollama show --verbose
```text


[stderr]
ollama not found
```

#### ollama show
```text


[stderr]
ollama not found
```

### qwen3:14b

#### ollama show --verbose
```text


[stderr]
ollama not found
```

#### ollama show
```text


[stderr]
ollama not found
```

### gemma3:12b

#### ollama show --verbose
```text


[stderr]
ollama not found
```

#### ollama show
```text


[stderr]
ollama not found
```

### llama3.1:8b

#### ollama show --verbose
```text


[stderr]
ollama not found
```

#### ollama show
```text


[stderr]
ollama not found
```

### mistral-small3.1:24b

#### ollama show --verbose
```text


[stderr]
ollama not found
```

#### ollama show
```text


[stderr]
ollama not found
```

